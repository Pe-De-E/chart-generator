# Run unit tests before pushing (~20s)
# E2E tests run in CI — too slow and infrastructure-dependent for local hooks
# Skip with: git push --no-verify
pnpm --filter @chart-generator/frontend test:coverage
